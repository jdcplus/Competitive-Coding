#include <bits/stdc++.h>

using namespace std;

int main(void) {
  ios::sync_with_stdio(false);

  long long a, b;
  while (cin >> a >> b){
    cout<<abs(a-b)<<endl;
    }
        
  return 0;
}